# 结构收敛第二批：clarify 与 focus 整理

- 最后修改时间：2026-06-02 14:49:00
- 文档定位：第三阶段 P0 结构收敛第二批记录
- 上级入口：[Optimize/README.md](/D:/Desktop/SGG_Project/Ecommerce_Customer_Service/Optimize/README.md)
- 下级入口：[SESSION_HANDOFF.md](/D:/Desktop/SGG_Project/Ecommerce_Customer_Service/SESSION_HANDOFF.md)

## 本轮目标

- 继续做结构收敛，不新增功能
- 优先整理 `clarify/` 的策略分支
- 继续整理 `focus/` 的文本对象切换兜底
- 轻量复查 `turns/plan` 边界表达

## 为什么现在做

- 第一批已经把 `engine` 主链拆成了薄编排器 + 回合处理器
- 下一步最自然的收口点就是 `clarify` 和 `focus` 这两块仍偏“堆在一起”的内部逻辑
- 当前回归基线已经稳定，不需要为了单个 case 再开行为补丁

## 实际改动

1. 在 [clarify/responder.py](/D:/Desktop/SGG_Project/Ecommerce_Customer_Service/customer-service-backend/atguigu/clarify/responder.py) 中：
   - 把“上下文优先提示”和“按 reason 返回提示”分成两层
   - 收出：
     - `_build_contextual_clarify_message`
     - `_build_reason_clarify_message`
     - `_build_missing_track_clarify_message`
     - `_build_missing_task_commands_message`
     - `_build_invalid_task_commands_message`
   - 把多处 `focused_object.type` 判断压成 `_get_focused_object_type`
   - 保持现有对外语义不变
2. 在 [engine/focus/text_switch.py](/D:/Desktop/SGG_Project/Ecommerce_Customer_Service/customer-service-backend/atguigu/engine/focus/text_switch.py) 中：
   - 把三种匹配规则统一到 `_find_unique_candidate`
   - 收出：
     - `_matches_candidate_id`
     - `_matches_candidate_exact_title`
     - `_matches_candidate_title_contains`
   - 让“兜底匹配规则”比之前更集中、更容易降级
3. 在 [plan/turn_validator.py](/D:/Desktop/SGG_Project/Ecommerce_Customer_Service/customer-service-backend/atguigu/plan/turn_validator.py) 中：
   - 收出 `ALLOWED_TASK_COMMAND_TYPES`
   - 收出 `_collect_start_commands`
   - 收出 `_requires_missing_object`
   - 仅整理表达，不改校验规则

## 验证结果

- 语法级校验通过：
  - `clarify/responder.py`
  - `engine/focus/text_switch.py`
  - `plan/turn_validator.py`
- 项目 `.venv` 真实导入校验通过：
  - `build_dialogue_engine()`
  - `DialogueEngine`
  - `TextTurnHandler`
  - `ObjectTurnHandler`
- 正式回归：
  [functional-test-workspace/reports/20260602_144549/report.md](/D:/Desktop/SGG_Project/Ecommerce_Customer_Service/functional-test-workspace/reports/20260602_144549/report.md)
- 结果摘要：
  - API：`pass 11 / behavior_fail 0 / infra_fail 0`
  - Browser：`pass 0 / behavior_fail 0 / infra_fail 0`
  - 本轮仍使用：
    - `SKIP_BROWSER_SMOKE=1`
    - `SKIP_PROPERTY_BACKEND_PRECHECK=1`

## 与前端 / 中台的对接影响

本轮仍然没有改前端或中台接口。

1. 前端无需改接口
2. 中台无需改字段或请求方式
3. 改动范围继续限定在后端内部结构整理

## 未完成项

1. `text_switch.py` 仍是 `focus/` 里最长的文件，但已经从重复循环收成统一匹配套路
2. browser smoke 这一轮仍未真实执行
3. `processor.py` / `executor.py` 依旧没有提级，继续维持当前判断

## 下一步优先级

1. 继续结构收敛第三批：
   - 复查 `focus/` 内是否还有可降级的兜底逻辑
   - 复查 `turns/plan` 是否还有重复协议判断
2. 保持不追单个 badcase，不因为局部回退冲动加补丁
3. 结构收敛告一段落后，再统一复盘哪些模块还能继续合并或下沉
