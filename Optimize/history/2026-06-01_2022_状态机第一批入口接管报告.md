# 状态机第一批入口接管报告

- 最后修改时间：2026-06-01 20:22:00

## 本轮目标

1. 不再继续围绕 badcase 零散补分支。
2. 在不大拆业务动作层的前提下，把显式会话状态和最近一次状态转移记录接进主链。
3. 先接管退出、对象承接、task/knowledge/chitchat 入口分流。

## 为什么现在做

第三阶段已经明确切到方案 B。再往下如果没有显式状态和转移记录，后续所有“为什么没退出”“为什么还黏着旧对象”“为什么已经进任务了却看不出来”都会继续变成靠猜的排错。

## 实际改动

- 在 [customer-service-backend/atguigu/domain/state.py](/D:/Desktop/SGG_Project/Ecommerce_Customer_Service/customer-service-backend/atguigu/domain/state.py) 中新增：
  - `ConversationState`
  - `ConversationTransition`
  - `conversation_state`
  - `last_transition`
  - `transition_to / recompute_conversation_state / reset_runtime_state`
- 在 [customer-service-backend/atguigu/engine/dialogue_engine.py](/D:/Desktop/SGG_Project/Ecommerce_Customer_Service/customer-service-backend/atguigu/engine/dialogue_engine.py) 中重写入口处理顺序：
  - 先判 runtime exit
  - 再做 turn plan 与 validator
  - 再显式决策 `task / knowledge / chitchat`
  - object message 走单独的 handoff / clarify 路径
- 把 `system_collect_information` 对应的状态语义修正为 `clarifying`，不再一直标成 `transitioning`。

## 回归结果

- 编译级校验通过：
  - `customer-service-backend/atguigu/domain/state.py`
  - `customer-service-backend/atguigu/engine/dialogue_engine.py`
- 定向接口烟测通过：
  1. 工单对象 -> `取消`
     - `conversation_state = idle`
     - `last_transition.event = runtime_exit_requested`
  2. 工单对象 -> `帮我催办一下`
     - `active_task.flow_id = work_order_urge_submission`
     - `conversation_state = clarifying`
- `npm run precheck` 对本地助手接口通过，但外部物业中台不可达，随后 Node 进程出现断言退出。

## 未完成项

1. task 分支里的 `reason` 还比较粗，存在 `set_slots` / `start_flow` 谁覆盖谁的问题。
2. 还没有把 track 观测做成更明确的测试断言字段。
3. 还没正式跑一轮长对话回归集。

## 下一步优先级

1. 继续收口 task transition 语义。
2. 挑选长对话 case 跑第一轮状态机接管回归。
3. 再决定第二批接管 resume / cancel / switch 的更细粒度动作边界。
