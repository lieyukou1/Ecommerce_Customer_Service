# 2026-06-06 17:00 TurnPlan 微调数据集 Phase 1

## 本轮目标

- 为物业客服项目补齐一套可训练、可校验、可扩展的 TurnPlan 微调数据集底座。
- 只覆盖 TurnPlan 路由决策，不进入最终客服回复微调。

## 为什么现在做

- 当前项目已经完成第三阶段主链重构，进入面试准备与能力扩展阶段。
- 如果后续要做“小模型替代大模型做路由器”，先把数据协议、样本结构和离线验收口径定死，比先碰训练脚本更值钱。

## 实际改动

- 新建 `Material/Datasets/turnplan-phase1/` 工作区。
- 增加：
  - 数据集 README
  - 标注规范
  - 离线评测口径
  - Phase 2 历史对话接入规范
  - canonical record schema
  - 数据集生成脚本
  - 静态校验脚本
  - SFT 导出脚本
- 计划生成：
  - `450 train + 80 val`
  - `canonical + SFT export`

## 回归结果

- 本轮不涉及后端主链行为代码，不跑业务回归。
- 本轮验收改为：
  - 数据生成
  - 静态 schema / 规则校验
  - SFT 导出校验
- 当前结果：
  - 已生成 `450 train + 80 val`
  - `validate_dataset.py` 校验通过
  - `export_sft.py` 导出通过

## 未完成项

- 真实历史对话还未接入，留到 Phase 2。
- 训练脚本、训练参数、模型选型实验还未展开。

## 下一步优先级

1. 跑通本批数据生成与校验
2. 人工抽样复核黄金样本
3. 如需继续推进，再进入 Phase 2 历史对话接入
