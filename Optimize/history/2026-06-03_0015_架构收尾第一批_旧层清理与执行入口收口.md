# 架构收尾第一批：旧层清理与执行入口收口

- 最后修改时间：2026-06-03 00:15
- 文档定位：第三阶段 P0 架构收尾第一批记录
- 上级入口：[Optimize/README.md](/D:/Desktop/SGG_Project/Ecommerce_Customer_Service/Optimize/README.md)
- 下级入口：[SESSION_HANDOFF.md](/D:/Desktop/SGG_Project/Ecommerce_Customer_Service/SESSION_HANDOFF.md)

## 本轮目标

- 开始执行“架构收尾四项”
- 先清掉旧层残影
- 先收一轮 `track_execution` 的 wiring 和公开入口
- 顺手降低 `focus/` 的高层存在感

## 为什么现在做

- 当前已经不需要继续纠缠单点回归
- 现在最重要的是让代码树、架构图和阅读心智模型一致
- 这一步做完，后面的收尾会更轻，也更适合转面试材料

## 实际改动

1. 清理旧层残影
   - 删除 `engine/runtime`
   - 删除 `engine/turns`
   - 清掉 `engine/` 下历史 `__pycache__`
2. 收 `track_execution` wiring
   - 新增 [runtime.py](/D:/Desktop/SGG_Project/Ecommerce_Customer_Service/customer-service-backend/atguigu/engine/track_execution/runtime.py)
   - 引入 `TrackExecutionRuntime`
   - 引入 `build_track_execution_runtime()`
   - 把 `TaskCommandExecutor + TextTurnHandler + ObjectTurnHandler` 的拼装收回 `track_execution/`
3. 简化 [dialogue_engine.py](/D:/Desktop/SGG_Project/Ecommerce_Customer_Service/customer-service-backend/atguigu/engine/dialogue_engine.py)
   - 顶层不再亲手组装执行层细件
   - 改为只依赖 `build_track_execution_runtime()`
4. 收窄包级入口
   - [track_execution/__init__.py](/D:/Desktop/SGG_Project/Ecommerce_Customer_Service/customer-service-backend/atguigu/engine/track_execution/__init__.py) 只保留 `TrackExecutionRuntime` 和 `build_track_execution_runtime`
   - [focus/__init__.py](/D:/Desktop/SGG_Project/Ecommerce_Customer_Service/customer-service-backend/atguigu/engine/focus/__init__.py) 只暴露 `FocusedObjectResolver`
5. 降低 `focus/` 的高层存在感
   - [resolver.py](/D:/Desktop/SGG_Project/Ecommerce_Customer_Service/customer-service-backend/atguigu/engine/focus/resolver.py) 内部 helper 成员改为私有前缀
6. 小幅内聚 `state_decision`
   - [engine.py](/D:/Desktop/SGG_Project/Ecommerce_Customer_Service/customer-service-backend/atguigu/engine/state_decision/engine.py) 新增 `should_execute_task_turn()`
   - `text_turn_handler.py` 不再直接依赖 `SemanticKind` 枚举做执行判断

## 回归结果

- `python -m compileall atguigu/engine` 通过
- 当前这批没有继续扩散到执行层和前端
- 目录树已经更接近“6 个一级概念”心智模型

## 未完成项

1. `state_decision` 仍可再收一轮中间协议暴露面
2. `focus/` 仍保留内部策略文件，但高层存在感已经明显下降
3. 最终架构图和收口报告还没出

## 下一步优先级

1. 再看 `state_decision` 是否还存在不必要的外露中间概念
2. 判断这轮是否已经足够收口，还是只需要再做一小刀
3. 然后准备最终架构图和面试材料入口
