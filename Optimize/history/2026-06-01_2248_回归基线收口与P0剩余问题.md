最后修改时间：2026-06-01 22:48

# 本轮目标

继续第三阶段 P0，收口长对话回归基线中的旧预期噪音，重新执行回归，确认状态机第一批接管后真正剩余的 P0 问题。

# 为什么现在做

上一轮报告里混着两类失败：

1. 真实的状态流转问题
2. 回归场景仍按旧语义断言，导致的假失败

如果不先把第二类清掉，后续会继续被测试噪音带偏，无法稳定判断下一刀该落在状态机入口、任务切换，还是仅仅该改测试。

# 实际改动

1. 调整 `functional-test-workspace/scenarios/api-scenarios.js`
   - 让“退出后新话题自动重新聚焦对象”的场景不再按旧逻辑要求 `focused_object = null`
   - 让部分“对象首轮进入”场景接受 `clarify` 作为合法轨道
   - 修正若干退出类场景的最终状态断言

2. 重新执行长对话回归
   - 命令：
     `SKIP_PROPERTY_BACKEND_PRECHECK=1`
     `SKIP_BROWSER_SMOKE=1`
     `node .\scripts\cli.js run`
   - 产物目录：
     `D:\Desktop\SGG_Project\Ecommerce_Customer_Service\functional-test-workspace\reports\20260601_224032`

# 回归结果

本轮结果：

- `pass`: 5
- `placeholder_pass`: 0
- `behavior_fail`: 6
- `infra_fail`: 0

相对上一轮，回归噪音已经明显下降，当前没有新的 500 / infra fail。

本轮确认通过的场景：

- `track-switch_chitchat-knowledge-task_followed-by-reset`
- `object-switch_service-item-a-to-b_and-exit`
- `task-switch_progress-to-urge-to-complaint`
- `exit-and-reset_followup-exit-then-reenter`
- `exit-and-reset_task-exit-then-start-new-object`

# 未完成项

当前剩余 6 个 `behavior_fail`，其中可分两类：

## A. 更像真实 P0 的状态流转问题

1. `track-switch_knowledge-clarify-task_work-order-followup`
   - 在“那先帮我催一下，家里老人怕热”这一轮：
     - `last_transition.reason = start:work_order_urge_submission`
     - 但 `active_task = null`
     - 实际轨道落到 `knowledge`
   - 下一轮“原因就是家里老人怕热，希望今天能处理”甚至落到 `chitchat`
   - 这说明“知识跟进 -> 任务启动 -> 槽位补齐”的衔接仍不稳，是当前最值得优先看的 P0。

2. `object-switch_service-item-to-work-order_with-task`
   - 仍按旧场景要求把对象首轮直接判成 `knowledge`
   - 这里需要决定：该场景是继续放宽为 `clarify/knowledge`，还是认为当前语义不符合预期。
   - 从状态机第一批设计看，更偏向前者，但还需要统一回归口径。

## B. 更像回归断言仍偏死的场景

1. `track-switch_chitchat-knowledge-task_service-item-to-work-order`
   - 只剩寒暄首轮的话术断言过死

2. `track-switch_knowledge-clarify-task_service-item-to-business`
   - “我就是想直接办一下”这轮实际进入 `human_handoff` 语义
   - 失败点主要是期望话术片段不匹配，不像状态错乱

3. `object-switch_work-order-a-to-b_followup`
   - 两个失败点都在对象首轮澄清话术没命中预设关键词

4. `task-switch_urge-then-complaint-on-another-work-order`
   - 只剩首轮对象澄清话术断言过死

# 下一步优先级

1. 先处理 `track-switch_knowledge-clarify-task_work-order-followup`
   - 这是当前最像真 bug 的链路
   - 重点看“任务启动意图已识别，但没有落成 active_task”的入口衔接

2. 再统一“对象首轮进入”的回归断言口径
   - 明确哪些场景应接受 `clarify`
   - 把剩余纯话术型误报继续清掉

3. 回归复跑
   - 目标是把当前 6 个失败继续压缩到只剩真正的状态机问题
