最后修改时间：2026-06-02 08:23

# 本轮目标

根据最新协作结论，停止把“剩余 2 个失败 case”当作当前主线，把第三阶段 P0 的重心正式切回：

1. 架构清理
2. 模块瘦身
3. 判定协议去关键词化

# 为什么现在做

虽然上一轮回归已经收敛到：

- `pass 9 / behavior_fail 2`

但这并不等于当前主线就该继续围着那 2 个 case 写下去。

这次重新审视后，问题更清楚了：

1. `dialogue_engine.py` 已经约 `789` 行
2. 当前如果继续按“盯两个失败 -> 继续补行为”推进，只会把 engine 写得更长
3. 代码主判定里仍然存在较强的直接关键词命中倾向
4. 这会把项目继续拉回：
   - 补丁化
   - 难解释
   - 难画架构图
   - 难长期维护

而这与第三阶段真正的目标是冲突的。

# 本轮结论

本轮正式提级以下约束为高优先级硬约束：

1. 单文件原则上不得超过 `450` 行
2. 单模块内不得出现 `5` 层及以上函数嵌套
3. 不以 badcase 命中率作为当前阶段唯一指标
4. 不允许把大段关键词命中继续作为主判定路径
5. 主判定应优先依赖：
   - LLM 结构化输出
   - 显式上下文决策协议
   - 状态机统一接管

# 排期调整

本轮已把排期重排为：

## 当前 P0

1. 方案 B 状态机架构清理与职责拆分
2. 判定协议去关键词化
3. 回归继续只作为门禁，不再单独驱动主线写补丁

## 后移事项

1. 剩余 2 个可疑 case 的定点分析
2. knowledge 连续追问优化
3. 局部上下文切换修补
4. 取消语义细分

这些都要等架构清理之后再回来看。

# 实际改动

本轮更新文档：

- `D:\Desktop\SGG_Project\Ecommerce_Customer_Service\Optimize\06_第三阶段优化计划.md`
- `D:\Desktop\SGG_Project\Ecommerce_Customer_Service\IMPLEMENTATION_PLAN.md`
- `D:\Desktop\SGG_Project\Ecommerce_Customer_Service\SESSION_HANDOFF.md`
- `D:\Desktop\SGG_Project\Ecommerce_Customer_Service\PROJECT_CONTEXT.md`

核心变化：

1. 把“架构清理与职责拆分”提升为新的前置 P0
2. 把“判定协议去关键词化”提升为新的前置 P0
3. 明确写入：
   - 不追求命中率压过架构清晰度
   - 不再接受继续把 engine 堆长

# 当前对实现方向的约束

接下来如果继续写代码，默认方向应当是：

1. 先拆 `dialogue_engine.py`
2. 把 route / transition / task finalize / context decision 等职责拆开
3. 先缩短主链，再决定哪里保留兜底规则
4. 关键词命中只能保留在极小范围保护逻辑中，不能继续做主判定

# 下一步优先级

1. 先做 engine 模块拆分设计
2. 明确哪些职责可以拆出到独立模块且不超过 `450` 行
3. 梳理当前主判定里哪些部分属于“直接词汇命中”
4. 设计新的 LLM 结构化判定协议承接这些职责

# 本轮不做

1. 不继续围绕剩余 2 个失败 case 扩写 engine
2. 不继续新增补丁式局部规则
3. 不把短期命中率提升当作架构优化完成的信号
