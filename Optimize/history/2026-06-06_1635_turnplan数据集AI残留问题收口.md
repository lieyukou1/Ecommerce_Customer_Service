# 2026-06-06 16:35 TurnPlan 数据集 AI 残留问题收口

## 本轮目标

- 收掉 AI 复审里剩下的 3 个问题：
  - query 类 flow 样本量偏低
  - `complaint_confirm` 没有否定样本
  - `contact_phone` 槽位覆盖太少

## 为什么现在做

- 数据集已经具备训练基础，但这 3 个点会直接影响后面的人工抽检重点
- 用户已经明确：这一轮不用再追求花哨数字，但要把真正影响语义边界的盲点补上
- 这一步做完，才能更放心地进入人工抽检和真实手动对话采样

## 实际改动

1. 重新平衡 bucket：
   - `work_order_read_only_task`：`55/10 -> 85/16`
   - `work_order_business_urge`：`45/8 -> 30/5`
   - `work_order_business_complaint`：`45/8 -> 30/5`
2. 重新平衡 query flow 配额：
   - 让 6 个 read-only / query flow 的覆盖更接近均衡，不再把增量都堆给某一个 flow
3. 给 `active_task_slot_fill` 增加显式 `contact_phone` 补槽样本：
   - 催办联系电话
   - 投诉联系电话
4. 给 `complaint_confirm` 增加明确否定样本：
   - 当前仍处在投诉确认步骤
   - 用户语义是“不提交 / 撤回 / 先停掉”
   - 输出改为 `cancel_flow`
5. 在 `metrics.py / validate_dataset.py / audit_dataset.py` 中新增专项观测：
   - `contact_phone_slot_records`
   - `complaint_confirm_negative_records`
6. 强化 `work_order_read_only_task` 的模板轮转与尾句，避免在扩大 query 配额后重新出现重复样本

## 回归结果

- `build_dataset.py`：通过
- `validate_dataset.py`：通过
- `export_sft.py`：通过
- `audit_dataset.py`：通过

关键结果：
- 精确重复对：`0`
- query 类 flow 分布：
  - `work_order_status_query`: `18`
  - `service_progress_tracking`: `17`
  - `service_item_detail_query`: `17`
  - `resident_work_orders_list_query`: `17`
  - `resident_service_items_list_query`: `16`
  - `resident_rule_qa`: `16`
- `work_order_urge_submission`: `61`
- `complaint_request_submission`: `48`
- `contact_phone` 槽位记录：`23`
- `complaint_confirm` 否定样本：`15`

## 未完成项

- 还没有进入人工抽检
- 还没有做真实手动对话采样
- 还没有把真实历史对话抽取成 `history-backed` 正式样本

## 下一步优先级

1. 用户做人工抽检
2. 人工抽检通过后，用户做真实手动对话
3. 对真实对话与历史状态数据做抽取、清洗、整理
4. 把 `history-backed` 样本并入同一 canonical 结构，进入 Phase 2
