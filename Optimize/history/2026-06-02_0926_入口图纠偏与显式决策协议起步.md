# 2026-06-02 09:26 入口图纠偏与显式决策协议起步

## 本轮目标

1. 把下一步排期正式写回第三阶段文档
2. 把 `PROJECT_CONTEXT.md` 收回到稳定入口图角色
3. 开始状态机第二批接管的第一步：显式决策协议

## 为什么现在做

上一轮的主要问题已经不是“能不能跑”，而是：

- 入口文档被我写得太像作战手册
- engine 虽然拆短了，但“这一轮到底是退出、查询还是办理”还没有被正式抽象成协议

如果继续直接改行为，后面还是会重新长回补丁结构。

## 实际改动

1. 更新排期文档
   - `Optimize/06_第三阶段优化计划.md`
   - `IMPLEMENTATION_PLAN.md`
   - `SESSION_HANDOFF.md`
2. 重写 `PROJECT_CONTEXT.md`
   - 只保留：
     - 构建/运行入口
     - 关键架构约束
     - 按任务类型跳转文档
   - 保留硬约束，不再混入阶段性执行细节
3. 新增显式决策协议起点
   - `engine/turn_decision_protocol.py`
   - `task/flow/semantics.py`
4. 调整 `engine/dialogue_engine.py`
   - 不再自己拼文本轮次的主决策
   - 改由 `TurnDecisionProtocol` 负责把 `TurnPlan` 解释成显式语义类别和 route decision
5. 调整 `engine/turn_models.py`
   - `TextTurnContext` 新增 `semantic_kind`

## 当前结构结果

- `PROJECT_CONTEXT.md` 当前约 `51` 行
- `dialogue_engine.py` 当前约 `335` 行
- `turn_decision_protocol.py` 当前约 `110` 行

## 校验结果

- `compileall` 已通过：
  - `engine/*`
  - `plan/*`
  - `task/flow/*`
- 已做最小本地接口联调：
  - `POST /api/chat`
  - 新协议层未造成导入或主链崩溃

## 本轮结论

这轮完成的是“把下一步要抽的东西先放到正确位置上”，不是行为全面收口：

1. 入口图角色已纠偏
2. 显式决策协议已起步
3. engine 主链继续变薄
4. 剩余的对象切换与状态机边界收紧，留到下一轮继续推进

## 下一步优先级

1. 继续完善显式决策协议
2. 开始拆 `focused_object_resolver.py`，分离：
   - 对象候选来源
   - 对象承接规则
   - 文本对象切换策略
3. 再收紧状态机内部的观测表达职责
