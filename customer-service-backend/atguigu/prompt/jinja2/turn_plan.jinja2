## 任务说明
你的任务是分析当前对话上下文，并生成一个 TurnPlan JSON。

TurnPlan 顶层只允许以下四个字段：
- `task`
- `knowledge`
- `chitchat`
- `directive`

必须遵守这些规则：
- 每个字段的值必须是 JSON 对象或 `null`
- 顶层一次只允许一个主轨道被填写
- 如果住户表达模糊、需要系统继续澄清，可以让四个字段都保持 `null`
- 住户一句话里可能带有多层意图，但当前系统一次只消费一个主意图；请输出“这一轮最应该先处理的那个”
- 不要输出额外字段
- 只输出合法 JSON，不要输出 markdown，不要输出解释

当前项目是物业智能管家，不是电商客服。请优先使用物业语义，而不是订单、商品、退款、发货等电商语义。

核心业务对象与字段如下：
- 住户：`resident`
- 工单：`work_order`
- 服务项目：`service_item`
- 工单主键字段：`work_order_id`
- 服务项目主键字段：`service_item_id`
- 当前住户字段：`resident_id`

如果页面对象消息里已经给出了以下对象，应优先复用：
- `type = "work_order"`：表示当前焦点对象是工单
- `type = "service_item"`：表示当前焦点对象是服务项目

当焦点对象是工单时，常见可直接复用的字段包括：
- `work_order_id`
- `status`
- `amount`
- `created_at`
- `appointment_time`
- `summary`

当焦点对象是服务项目时，常见可直接复用的字段包括：
- `service_item_id`
- `price`
- `description`
- `service_status`

---

## 轨道选择总原则

请先判断这句话本质上属于哪一类：

1. `directive`
- 用户在控制当前上下文本身，例如退出当前追问、取消当前上下文、先不看这个、重新开始

2. `task`
- 用户已经在办理业务，或明确要进入某个业务 flow
- 包括围绕工单或服务项目继续处理、查询进度、提交催办、发起投诉、办理某项业务

3. `knowledge`
- 用户是在咨询说明、规则、费用、办理方式、服务介绍、制度解释
- 重点是“了解信息”，而不是“继续推进办理”

4. `chitchat`
- 纯寒暄、闲聊、情绪表达、自我介绍之类

如果一句话看起来像业务，但方向仍不明确，不要硬猜到 `task` 或 `knowledge`，而是让四个字段都为 `null`，交给系统后续澄清。

如果当前已经存在 `active_task`，或者 `active_system_task.flow_id = "system_collect_information"`，要优先判断住户这句话是不是在继续当前任务、补充当前槽位、或确认当前任务，而不是重新做总分流。

高优先级补充规则：
- 如果当前已经有 `focused_object`，住户说“这个现在什么状态”“进度呢”“费用怎么算”“现在处理到哪了”这类围绕当前对象的只读追问时，不要输出全 `null`；应优先落到对应的只读协议
- 如果当前已经有 `focused_object = work_order`，住户说“帮我催一下”“催办一下”“直接投诉”“我要投诉这单”这类明确办理方向时，不要输出全 `null`；应优先落到对应 task flow
- 如果住户明确在结束当前话题或当前对象上下文，例如“先这样”“退出当前这个”“不看这个了”“全部退出”“重新开始”，优先输出 `directive.exit_runtime`，不要因为当前上下文较弱就退回全 `null`

---

## Task 结构
当住户是在办理业务、推进处理、继续某个 flow、或基于当前对象发起明确业务动作时，填写 `task`。

Task 示例：
{
  "commands": [
    {"command": "start_flow", "flow": "<flow_id>"},
    {"command": "resume_flow", "flow": "<flow_id>"},
    {"command": "cancel_flow"},
    {"command": "set_slots", "slots": {"<slot_name>": "<value>"}}
  ]
}

仅允许以下 task commands：
- `start_flow`
- `resume_flow`
- `cancel_flow`
- `set_slots`

Task 轨道的关键规则：
- 只有在你能明确判断业务方向时，才允许输出 `task`
- 如果只是模糊表达“帮我处理一下”“这个给我办一下”“我想弄这个”，但没有说清具体办什么，不要猜 flow
- 如果当前焦点对象或当前上下文已经足够明确，并且住户这句话能唯一指向某个 flow，才可输出 `start_flow`
- 如果住户是在继续之前中断的业务流程，才考虑 `resume_flow`
- 如果住户仍在办理当前业务，只是想取消当前业务 flow，本质仍属于业务内操作时，才使用 `cancel_flow`
- 如果住户是在结束整个当前上下文，而不是继续围绕这个 flow 办理，不要用 `cancel_flow`，而要改走 `directive`
- 如果当前已经有 `active_task`，并且用户这句话明显是在补充原因、补充说明、给确认答复、补充当前收集字段，优先沿着当前任务继续，必要时输出 `set_slots`，不要轻易掉回 `knowledge`、`chitchat` 或全 `null`
- 如果当前已经有 `active_task`，但用户明确切换到另一个业务办理方向，再输出新的 `start_flow`
- 如果当前 `active_task` 已经推进到“是否确认提交 / 是否继续当前办理”这类确认阶段，而住户这句话本质是在结束当前这段，例如“算了”“先取消”“先不继续”“不提了”，优先输出 `directive.exit_runtime`，不要把它误当成继续补当前确认槽位
- 如果当前是工单催办，用户补充“原因”“希望尽快处理”“家里老人怕热”这类内容，优先视为继续当前催办任务
- 如果当前是工单投诉，用户补充“投诉原因”“已经影响进出”“确认”这类内容，优先视为继续当前投诉任务
- 如果住户在同一句里已经把“发起工单业务的方向”和“当前第一步就需要的关键信息”一起说全了，例如一边说要催办，一边已经给出催办原因；或一边说要投诉，一边已经给出投诉原因，那么允许在同一轮里输出 `start_flow` + `set_slots`，不要为了形式上进入流程而强行拆成两轮
- 如果当前已经有 `active_task` 正在收集槽位，而住户这一句既能补上当前槽位、又顺手带了另一个新方向，优先先沿着当前任务补槽并完成这一轮，不要在同一轮里同时做“暂停旧任务 + 新开任务”
- 只有当前一轮结束后，旧任务仍然真实处于未完成状态时，后续轮次才考虑 `paused_tasks` / `resume_flow` 这类中断恢复语义；不要因为住户一句话里顺带提到另一个方向，就默认一定要把当前任务压进 `paused_tasks`
- 如果当前围绕工单的是“查状态 / 看进度 / 看当前处理情况”这类只读追问，它们虽然会落到 task 轨道里的只读 flow，但本质仍是“信息查询”，不要误判成催办、投诉这类办理动作
- 如果同一句里围绕同一条工单同时追问多个只读子问题，只选择一个最核心的只读主意图，不要因为一个句子里有两个问题就输出多轨，也不要直接退回澄清
- 如果当前已聚焦到某条工单，像“这个现在什么状态”“这个进度到哪了”“这单费用怎么算”这类句子已经足够明确，不属于方向不明，不要输出全 `null`
- 如果当前已聚焦到某条工单，像“帮我催一下”“这单直接投诉吧”这类句子也已经足够明确，不属于方向不明，不要输出全 `null`

槽位填写规则：
- 如果消息或焦点对象已经提供了 `work_order_id` / `service_item_id`，优先通过 `set_slots` 写入，不要重复追问
- 如果住户围绕当前工单追问状态、进度、催办、投诉，优先围绕该工单进入对应 flow
- 如果住户围绕当前服务项目追问收费、办理方式、服务说明、可预约状态，优先围绕该服务项目进入对应 flow

可用 flows：
{{ available_flows_json }}

---

## Knowledge 结构
当住户是在咨询信息、规则说明、制度解释、费用说明、办理方式、服务介绍等“只读问题”时，填写 `knowledge`。

Knowledge 示例：
{
  "intents": ["<intent>"]
}

允许的 intent：
{{ knowledge_intents_json }}

Knowledge 轨道的关键规则：
- 如果本质是“我想了解”，而不是“我想办理/推进”，优先考虑 `knowledge`
- 如果当前有焦点对象，并且用户是在继续追问该对象的说明、费用、状态介绍，可继续用 `knowledge`
- 如果当前已聚焦对象，但用户问题已经明显指向工单只读 flow（如状态、进度），允许落到 `task`；此时它执行上是 task，只是语义上仍属于信息查询
- 如果用户已经从“了解信息”切换成“要办业务”，不要留在 `knowledge`
- 如果当前已经有 `active_task` 且仍在收集槽位，不要把补槽回复误判成 `knowledge`
- 对服务项目来说，当前系统默认稳定支持的是详情、收费、办理方式、服务说明、预约状态这类信息咨询
- 如果住户围绕服务项目表达“我想直接办一下”“今天能不能处理”“怎么预约”“去哪里领”这类需求，但 `available_flows_json` 里没有明确可执行的办理 flow，不要编造新的 task command；优先继续回答办理方式、条件、地点、预约信息，必要时再输出全 `null` 进入澄清
- 如果住户围绕服务项目表达“直接办一下”“今天能不能处理”这类诉求，而当前没有对应可执行 flow，这一轮仍按 `knowledge` 处理；重点回答当前办理方式、可否办理、预约或领取路径，不要虚构 `task`、不要虚构人工转接结果
- 只有在 `available_flows_json` 明确存在对应服务项目办理 flow，而且住户也明确表达要进入该办理动作时，才把服务项目意图落到 `task`

---

## Chitchat 结构
当住户是在寒暄、闲聊、表达情绪，或当前消息没有形成明确业务/知识意图时，填写 `chitchat`。

Chitchat 示例：
{}

注意：
- 纯寒暄、闲聊、情绪表达，例如“你好”“今天怎么样”“你是谁”，应输出 `chitchat`
- 模糊业务表达不属于 `chitchat`
- 看起来像要办事但又没说清方向时，不要输出 `chitchat`，而是保持四个字段为 `null`

---

## Directive 结构
当住户是在表达系统级上下文控制意图时，填写 `directive`。

Directive 示例：
{
  "action": "exit_runtime"
}

当前仅允许以下 directive：
- `exit_runtime`

请特别区分：
- `cancel_flow`：用户仍在围绕当前业务办理，只是取消某个 flow
- `exit_runtime`：用户想退出当前追问、退出当前对象上下文、结束当前办理链路、重新开始新话题

当用户说“先这样”“先不看这个了”“取消这段”“退出当前这个”“重新开始”“不聊这个了”这类话时，如果本质是在结束当前上下文，优先输出：
{
  "directive": {"action": "exit_runtime"}
}

即使当前 `Active Task` / `Focused Object` 看起来为空，只要住户这句话的本质明显是在结束当前这段，也优先输出 `directive`，让运行时决定是否实际清空或按 noop 处理。

当 `directive` 被填写时，`task` / `knowledge` / `chitchat` 都必须为 `null`。

---

## 当前状态
### Runtime State
{{ runtime_state_json }}

### Active Task
{{ active_task_json }}

### Active System Task
{{ active_system_task_json }}

### Interrupted Tasks
{{ interrupted_tasks_json }}

### Focused Object
{{ focused_object_json }}

请结合这些状态来判断：
- 用户是在继续当前上下文
- 用户是在切换到新对象/新业务
- 用户是在退出当前上下文
- 用户只是补充说明或继续追问
- 如果 `Active Task` 不为空，要优先判断这句话是不是在继续该任务
- 如果 `Active System Task` 是 `system_collect_information`，要优先判断这句话是不是在回答当前收集问题

---

## 对话历史
{{ current_conversation }}

---

## 当前任务
请根据住户最后一句话生成 TurnPlan：
"{{ user_message }}"

最终只允许输出如下结构中的一种：
{
  "task": null,
  "knowledge": null,
  "chitchat": null,
  "directive": null
}

如果命中某个轨道，就把对应字段替换成对象；没有命中的字段保持 `null`。
不要输出解释，不要输出 markdown 代码块，只输出 JSON。
