# 10-物业语义对象与接口映射图

## 这册看什么

这一册专门做对照：

1. 老师电商语义怎么映射到当前物业语义
2. 前端对象 payload 长什么样
3. 后端要承接哪些物业中台接口

## 图 1：电商语义 -> 物业语义对象映射图

```mermaid
flowchart LR
    user["user"] --> resident["resident"]
    sender["sender_id"] --> resident_id["resident_id"]
    order["order"] --> work_order["work_order"]
    order_id["order_id"] --> work_order_id["work_order_id"]
    product["product"] --> service_item["service_item"]
    product_id["product_id"] --> service_item_id["service_item_id"]
    shipping["shipping_reminder"] --> urge["work_order_urge_request"]
    refund["refund_request"] --> complaint["complaint_request"]
```

## 图 2：前端对象 payload 图

```mermaid
classDiagram
    class ChatRequest {
      +resident_id: str
      +message_id: str | None
      +text: str | None
      +object: ChatObject | None
    }

    class ChatObject {
      +type: str
      +id: str
      +title: str | None
      +attributes: dict
    }

    ChatRequest --> ChatObject
```

## 图 3：智能管家到物业中台接口映射图

```mermaid
flowchart TD
    CSB["customer-service-backend"] --> Status["GET /work-orders/{work_order_id}/status"]
    CSB --> Progress["GET /work-orders/{work_order_id}/progress"]
    CSB --> Detail["GET /service-items/{service_item_id}"]
    CSB --> WorkOrders["GET /residents/{resident_id}/work-orders"]
    CSB --> ServiceItems["GET /residents/{resident_id}/service-items"]
    CSB --> Urge["POST /work-orders/{work_order_id}/urge-requests"]
    CSB --> Complaint["POST /work-orders/{work_order_id}/complaint-requests"]
```

## 对照表

| 老师电商语义 | 当前物业语义 | 当前项目落点 |
| --- | --- | --- |
| `user` | `resident` | `resident_id` |
| `sender_id` | `resident_id` | 会话身份口径 |
| `order` | `work_order` | 工单对象 / 工单接口 |
| `product` | `service_item` | 服务项目对象 / 服务项目接口 |
| `order_id` | `work_order_id` | flow slot / object.id / HTTP path |
| `product_id` | `service_item_id` | flow slot / object.id / HTTP path |
| `shipping_reminder` | `work_order_urge_request` | 催办接口 |
| `refund_request` | `complaint_request` | 投诉 / 异议接口 |

## 一句话结论

当前物业版不是推翻老师骨架，而是把老师那套电商语义沿着 `resident / work_order / service_item` 重新落到真实前端和物业中台上。
