# 01-系统全景与三仓关系

## 这册看什么

这一册只回答三件事：

1. 为什么当前项目是三仓协作
2. 三个仓各自负责什么
3. 当前第二阶段的真实主链是什么

它不展开类、字段和方法。

## 图 1：系统全景

```mermaid
flowchart TB
    user["住户 / 前端操作者"]

    subgraph frontend["customer-service-frontend"]
        fe_chat["左侧聊天区"]
        fe_panel["右侧工单 / 服务项目面板"]
        fe_sender["text / object 消息发送"]
    end

    subgraph backend["customer-service-backend"]
        api["/api/chat"]
        engine["智能管家主体"]
        state["DialogueState / MySQL"]
    end

    subgraph commerce["ecommerce-service-backend"]
        commerce_api["物业中台 HTTP API"]
        commerce_db["property business MySQL"]
    end

    llm["LLM / OpenAI Compatible API"]

    user --> fe_chat
    user --> fe_panel
    fe_chat --> fe_sender
    fe_panel --> fe_sender
    fe_sender --> api
    api --> engine
    engine --> state
    engine --> llm
    engine --> commerce_api
    commerce_api --> commerce_db
```

## 图 2：三仓职责矩阵图

```mermaid
flowchart LR
    subgraph FE["customer-service-frontend"]
        FE1["承载聊天 UI"]
        FE2["承载对象消息入口"]
        FE3["发送 ChatRequest"]
    end

    subgraph CSB["customer-service-backend"]
        CSB1["接收 /api/chat"]
        CSB2["维护 DialogueState"]
        CSB3["规划 / 校验 / 澄清 / 分轨"]
    end

    subgraph ESB["ecommerce-service-backend"]
        ESB1["工单接口"]
        ESB2["服务项目接口"]
        ESB3["催办 / 投诉 / 进度接口"]
    end

    FE --> CSB --> ESB
```

## 图 3：前端 `/api` 与 `/commerce` 分流图

```mermaid
sequenceDiagram
    participant U as 住户
    participant FE as customer-service-frontend
    participant CSB as customer-service-backend
    participant ESB as ecommerce-service-backend

    U->>FE: 输入文本 / 点击工单或服务项目
    FE->>CSB: POST /api/chat
    CSB->>CSB: 规划 / 校验 / 状态更新
    alt 需要物业业务数据
        CSB->>ESB: GET/POST 物业中台接口
        ESB-->>CSB: 工单 / 服务项目 / 进度 / 投诉结果
    end
    CSB-->>FE: ChatResponse
    FE-->>U: 左侧自然语言回复
```

## 职责对照表

| 仓库 | 当前角色 | 主要输入 | 主要输出 | 当前状态 |
| --- | --- | --- | --- | --- |
| `customer-service-frontend` | 交互壳层 | 用户文本、对象点击 | `ChatRequest` | `[已实现]` |
| `customer-service-backend` | 智能管家主体 | `ChatRequest` | `ChatResponse`、状态持久化 | `[已实现]` 主骨架 |
| `ecommerce-service-backend` | 物业业务中台 | HTTP 查询 / 提交 | 工单、服务项目、进度、催办、投诉结果 | `[已实现]` |

## 一句话结论

当前阶段不是重做三仓，而是在已有前端壳和物业中台的前提下，把 `customer-service-backend` 做成真正的智能管家主体。
